import {StyleSheet} from 'react-native';

const Style = StyleSheet.create({
  overlay: {
    position: 'absolute',
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
  },
  wrapper: {flex: 1},
});

export default Style;
